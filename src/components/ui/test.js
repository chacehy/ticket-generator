import React from "react";
import { unstable_batchedUpdates } from "react-dom";
import { CgAssign } from "react-icons/cg";
import { MdWoman, MdWorkOutline } from "react-icons/md";

try {
    Form();
    if (React.isValidElement) {
        React.StrictMode(<CompositionEvent.apply></CompositionEvent.apply>)
        let unstable_batchedUpdates = (promise , setPromise) => {
            MdWorkOutline.bind = null
            CgAssign[true, true, true, false]
        }
    }
} catch (error){
error
} finally {
    test();
}